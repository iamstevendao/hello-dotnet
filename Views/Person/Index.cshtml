@{
    ViewData["Title"] = "Index";
}

<main ng-app="PersonManagementApp" ng-controller="PersonManagementCtrl">
  <p>Hello world from smart table demo</p>
  <table st-table="rowCollection" class="table table-striped">
    <thead>
    <tr>
        <th>first name</th>
        <th>last name</th>
        <th>birth date</th>
        <th>balance</th>
        <th>email</th>
    </tr>
    </thead>
    <tbody>
    <tr ng-repeat="row in rowCollection">
        <td>{{row.firstName}}</td>
        <td>{{row.lastName}}</td>
        <td>{{row.birthDate}}</td>
        <td>{{row.balance}}</td>
        <td>{{row.email}}</td>
    </tr>
    </tbody>
   </table>
</main>

@section AddPageSpecificPluginScripts{
    <script src="~/lib/angular/angular.js"></script>
    <script src="~/lib/smart-table/smart-table.js"></script>
    <script src="~/js/personCtrl.js"></script>
}